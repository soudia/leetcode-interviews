package com.interviews.microsoft;

import java.util.List;

public class MicrosoftInterviews {

	/**
	 * Given a set of 'n' intervals S = {(s_i, e_i); 1<= i <= n}, find a maximum
	 * subset of S such that no pair of intervals in 'S' overlap.
	 */
	public List<int[]> maxSubset(List<int[]> intervals) {
		return null;
	}

	/**
	 * Given a file, read last n lines from the file.
	 * 
	 */
	public String readLines(String fileName, int n) {
		return null;
	}

	/**
	 * Implement priority queue
	 */

	/**
	 * Given a string, left rotate it by given number of times with O(n)
	 * solution
	 */
	public String leftRotate(String str, int n) {
		return null;
	}

	/**
	 * Given an array of integers where all the numbers are distinct, write a
	 * program to find the two longest non-intersecting continuous sub-arrays of
	 * equal size such that all elements of one of them are smaller than that of
	 * the other subarray.
	 * 
	 * <pre>
	 * a[i .. i+k-1] and a[j .. j+k-1] where i + k <= j
	 * such that a[i .. i+k-1] < a[j .. j+k-1] or a[i .. i+k-1] > a[j .. j+k-1]
	 * and k is maximal
	 * 
	 * Example: a= {1, 2, 3, 7, 9, 8}, then we have: {1, 2, 3} and {7, 9, 8}
	 * a= {6, 5, 4, 1, 3, 7} then we have: 
	 * {6, 5} and {4, 1} or 
	 * {6, 5} and {1, 3} or
	 * {5, 4} and {1, 3} ...
	 * </pre>
	 */

	/**
	 * How do you traverse a binary tree and output the nodes in-order? Do it in
	 * O(1) space. Hint, you can modify the tree.
	 */
}
